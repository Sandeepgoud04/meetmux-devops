name: DevOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Test
        run: |
          echo "Running Tests"
          echo "Tests Passed"

      - name: Deploy
        run: |
          echo "Deploying to Dev Environment"
